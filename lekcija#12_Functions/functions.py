

def say_hello(name, surname):
    return("Hello {0} {1}!".format(name, surname))


def sum(a,b):
    return a + b

def calculate_gross(net, vat_rate_precentage):
    gross=net*(1 + vat_rate_precentage/100)

    return gross

def kebab(users_list):
    full_name_list=[]
    for x in user_list:
        full_name=x["name"] + "" + x["surname"]
        full_name_list.append[full_name]

        return full_name_list







